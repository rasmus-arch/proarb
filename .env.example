# Copy this file to .env and adjust as needed.

# MySQL connection (matches docker-compose.yml defaults)
DB_HOST=localhost
DB_PORT=3306
DB_USER=proarb
DB_PASSWORD=proarb
DB_NAME=proarb

# API
PORT=3001
